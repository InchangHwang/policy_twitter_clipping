# 환경변수 (API 키 포함 - 절대 업로드 금지)
.env

# 가상환경
venv/

# 실행 상태 파일
state.json

# 로그
*.log

# Python 캐시
__pycache__/
*.pyc
*.pyo

# Windows
Thumbs.db
desktop.ini

# AWS 자격증명 (절대 커밋 금지)
.aws/
credentials
*.pem
*.key

# detect-secrets
.secrets.baseline

# SAM 빌드 산출물
.aws-sam/
samconfig.toml

# Lambda 배포 패키지
*.zip
